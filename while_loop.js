// Using while loop to iterate over an array
let first_nums = 0;
let my_numbers = [2,9,1,5,8,10,34];

while (first_nums < my_numbers.length)
{
    console.log(my_numbers[first_nums]);
    first_nums++;
}